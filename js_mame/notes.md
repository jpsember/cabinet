From https://snapcraft.io/install/mame/raspbian

Not sure why 'sudo' is necessary...

```
sudo apt update
sudo apt install -y snapd

sudo reboot
```

```
sudo snap install core
sudo snap install mame

sudo snap install mame
```
